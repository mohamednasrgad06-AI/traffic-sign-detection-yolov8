# Models

This folder is intended for trained YOLOv8 model weights.

The trained YOLOv8n and YOLOv8m weights are generated during the training process and are not included directly in the repository if they exceed GitHub's file size limits.
